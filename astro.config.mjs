// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	site: 'https://faisalalnasser13.github.io',
	base: '/EntekhabiGroup/',
	output: 'static',
});
